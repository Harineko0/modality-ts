import type { TransitionRef } from "modality-ts/properties";

export const swr_billing_accountId_fetch = {
  // transitions
  _: {
    _: "swr:billing_accountId:fetch" as TransitionRef<"swr:billing_accountId:fetch">,
  },
};

export const swr_billing_accountId_resolve_error = {
  // transitions
  _: {
    _: "swr:billing_accountId:resolve:error" as TransitionRef<"swr:billing_accountId:resolve:error">,
  },
};

export const swr_billing_accountId_resolve_success_0 = {
  // transitions
  _: {
    _: "swr:billing_accountId:resolve:success:0" as TransitionRef<"swr:billing_accountId:resolve:success:0">,
  },
};

export const swr_invoice_invoiceId_fetch = {
  // transitions
  _: {
    _: "swr:invoice_invoiceId:fetch" as TransitionRef<"swr:invoice_invoiceId:fetch">,
  },
};

export const swr_invoice_invoiceId_resolve_error = {
  // transitions
  _: {
    _: "swr:invoice_invoiceId:resolve:error" as TransitionRef<"swr:invoice_invoiceId:resolve:error">,
  },
};

export const swr_invoice_invoiceId_resolve_success_0 = {
  // transitions
  _: {
    _: "swr:invoice_invoiceId:resolve:success:0" as TransitionRef<"swr:invoice_invoiceId:resolve:success:0">,
  },
};

export const swr_payment_methods_accountId_fetch = {
  // transitions
  _: {
    _: "swr:payment_methods_accountId:fetch" as TransitionRef<"swr:payment_methods_accountId:fetch">,
  },
};

export const swr_payment_methods_accountId_resolve_error = {
  // transitions
  _: {
    _: "swr:payment_methods_accountId:resolve:error" as TransitionRef<"swr:payment_methods_accountId:resolve:error">,
  },
};

export const swr_payment_methods_accountId_resolve_success_0 = {
  // transitions
  _: {
    _: "swr:payment_methods_accountId:resolve:success:0" as TransitionRef<"swr:payment_methods_accountId:resolve:success:0">,
  },
};
