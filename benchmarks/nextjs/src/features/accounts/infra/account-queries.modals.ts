import type { TransitionRef } from "modality-ts/properties";

export const swr_account_accountId_fetch = {
  // transitions
  _: {
    _: "swr:account_accountId:fetch" as TransitionRef<"swr:account_accountId:fetch">,
  },
};

export const swr_account_accountId_resolve_error = {
  // transitions
  _: {
    _: "swr:account_accountId:resolve:error" as TransitionRef<"swr:account_accountId:resolve:error">,
  },
};

export const swr_account_accountId_resolve_success_0 = {
  // transitions
  _: {
    _: "swr:account_accountId:resolve:success:0" as TransitionRef<"swr:account_accountId:resolve:success:0">,
  },
};
