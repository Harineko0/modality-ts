import type { TransitionRef } from "modality-ts/properties";

export const swr_dashboard_selectedAccount_fetch = {
  // transitions
  _: {
    _: "swr:dashboard_selectedAccount:fetch" as TransitionRef<"swr:dashboard_selectedAccount:fetch">,
  },
};

export const swr_dashboard_selectedAccount_resolve_error = {
  // transitions
  _: {
    _: "swr:dashboard_selectedAccount:resolve:error" as TransitionRef<"swr:dashboard_selectedAccount:resolve:error">,
  },
};

export const swr_dashboard_selectedAccount_resolve_success_0 = {
  // transitions
  _: {
    _: "swr:dashboard_selectedAccount:resolve:success:0" as TransitionRef<"swr:dashboard_selectedAccount:resolve:success:0">,
  },
};
