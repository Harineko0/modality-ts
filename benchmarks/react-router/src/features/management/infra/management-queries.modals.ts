import type { TransitionRef } from "modality-ts/properties";

export const swr_management_summary_fetch = {
  // transitions
  _: {
    _: "swr:management_summary:fetch" as TransitionRef<"swr:management_summary:fetch">,
  },
};

export const swr_management_summary_resolve_error = {
  // transitions
  _: {
    _: "swr:management_summary:resolve:error" as TransitionRef<"swr:management_summary:resolve:error">,
  },
};

export const swr_management_summary_resolve_success_0 = {
  // transitions
  _: {
    _: "swr:management_summary:resolve:success:0" as TransitionRef<"swr:management_summary:resolve:success:0">,
  },
};

export const swr_operations_queue_fetch = {
  // transitions
  _: {
    _: "swr:operations_queue:fetch" as TransitionRef<"swr:operations_queue:fetch">,
  },
};

export const swr_operations_queue_resolve_error = {
  // transitions
  _: {
    _: "swr:operations_queue:resolve:error" as TransitionRef<"swr:operations_queue:resolve:error">,
  },
};

export const swr_operations_queue_resolve_success_0 = {
  // transitions
  _: {
    _: "swr:operations_queue:resolve:success:0" as TransitionRef<"swr:operations_queue:resolve:success:0">,
  },
};

export const swr_revenue_queue_fetch = {
  // transitions
  _: {
    _: "swr:revenue_queue:fetch" as TransitionRef<"swr:revenue_queue:fetch">,
  },
};

export const swr_revenue_queue_resolve_error = {
  // transitions
  _: {
    _: "swr:revenue_queue:resolve:error" as TransitionRef<"swr:revenue_queue:resolve:error">,
  },
};

export const swr_revenue_queue_resolve_success_0 = {
  // transitions
  _: {
    _: "swr:revenue_queue:resolve:success:0" as TransitionRef<"swr:revenue_queue:resolve:success:0">,
  },
};

export const swr_risk_queue_riskBucket_fetch = {
  // transitions
  _: {
    _: "swr:risk_queue_riskBucket:fetch" as TransitionRef<"swr:risk_queue_riskBucket:fetch">,
  },
};

export const swr_risk_queue_riskBucket_resolve_error = {
  // transitions
  _: {
    _: "swr:risk_queue_riskBucket:resolve:error" as TransitionRef<"swr:risk_queue_riskBucket:resolve:error">,
  },
};

export const swr_risk_queue_riskBucket_resolve_success_0 = {
  // transitions
  _: {
    _: "swr:risk_queue_riskBucket:resolve:success:0" as TransitionRef<"swr:risk_queue_riskBucket:resolve:success:0">,
  },
};
