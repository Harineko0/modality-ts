import type { TransitionRef } from "modality-ts/properties";

export const swr_approvals_fetch = {
  // transitions
  _: {
    _: "swr:approvals:fetch" as TransitionRef<"swr:approvals:fetch">,
  },
};

export const swr_approvals_resolve_error = {
  // transitions
  _: {
    _: "swr:approvals:resolve:error" as TransitionRef<"swr:approvals:resolve:error">,
  },
};

export const swr_approvals_resolve_success_0 = {
  // transitions
  _: {
    _: "swr:approvals:resolve:success:0" as TransitionRef<"swr:approvals:resolve:success:0">,
  },
};

export const swr_subscription_accountId_fetch = {
  // transitions
  _: {
    _: "swr:subscription_accountId:fetch" as TransitionRef<"swr:subscription_accountId:fetch">,
  },
};

export const swr_subscription_accountId_resolve_error = {
  // transitions
  _: {
    _: "swr:subscription_accountId:resolve:error" as TransitionRef<"swr:subscription_accountId:resolve:error">,
  },
};

export const swr_subscription_accountId_resolve_success_0 = {
  // transitions
  _: {
    _: "swr:subscription_accountId:resolve:success:0" as TransitionRef<"swr:subscription_accountId:resolve:success:0">,
  },
};
