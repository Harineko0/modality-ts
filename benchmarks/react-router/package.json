{
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "../../node_modules/.bin/tsx ../../src/cli/cli.ts generate",
    "typecheck": "../../node_modules/.bin/tsc -p tsconfig.json --noEmit",
    "benchmark": "../../node_modules/.bin/tsx ../../tools/benchmark-ci.ts --id ledgerops-react-router",
    "verify": "pnpm generate && pnpm typecheck && pnpm benchmark"
  },
  "dependencies": {
    "arktype": "^2.1.0",
    "jotai": "^2.0.0",
    "react": "^18.0.0",
    "react-dom": "^18.0.0",
    "react-router-dom": "^6.0.0",
    "swr": "^2.0.0",
    "zustand": "^5.0.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/react": "^18.0.0",
    "@types/react-dom": "^18.0.0",
    "modality-ts": "file:../.."
  }
}
